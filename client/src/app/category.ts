export class Category {

  constructor(id: number, name: string, ageStrict: number) {
    this.id = id;
    this.name = name;
    this.ageStrict = ageStrict;
  }

  id: number;
  name: string;
  ageStrict: number;
}
